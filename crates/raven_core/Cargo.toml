[package]
name = "raven_core"
version = "3.2.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
async-trait = "0.1"
tokio = { version = "1.37", default-features = false, features = ["sync"] }
